# GitHub
GITHUB_TOKEN=ghp_your_token_here

# Server
PORT=3000

# =============================================================================
# AI Enhancement (Optional)
# =============================================================================
# whatsnew is deterministic-first: rule-based parsing runs before AI.
# AI only activates when parsing quality is low:
#   - Confidence < 0.6
#   - Most items end up in "other" category (>80%)
#   - No items extracted despite substantial content
#
# Set ONE of the API keys below to enable AI fallback.
# =============================================================================

# AI Provider API Keys (set ONE to enable)
# ANTHROPIC_API_KEY=sk-ant-...           # Direct Anthropic API
# OPENAI_API_KEY=sk-...                  # Direct OpenAI API
# AI_GATEWAY_API_KEY=your-gateway-key    # Vercel AI Gateway (multi-provider)

# AI Configuration
AI_PROVIDER=anthropic                    # anthropic or openai
# AI_MODEL=claude-3-haiku-20240307       # Override default model
AI_CONFIDENCE_THRESHOLD=0.6              # Quality threshold for AI fallback (0-1)
# AI_ENABLED=false                       # Explicitly disable AI even if key is set
